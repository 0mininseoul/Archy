import { formatKSTDate } from "./utils";

// =============================================================================
// Transcript Quality Analysis
// =============================================================================

export type TranscriptQuality = 'rich' | 'sparse' | 'minimal';

/**
 * 전사본의 품질을 분석하여 적절한 요약 전략을 결정합니다.
 * @param transcript 전사본 텍스트
 * @returns 'rich' | 'sparse' | 'minimal'
 */
export function analyzeTranscriptQuality(transcript: string): TranscriptQuality {
  const trimmed = transcript.trim();
  if (!trimmed) return 'minimal';

  const words = trimmed.split(/\s+/);
  const wordCount = words.length;

  // 필러 워드 및 무의미한 표현 패턴
  const fillerPatterns = [
    /^[음어그저아으]$/,
    /^\.{2,}$/,
    /^[음어그저아으]\.{2,}$/,
    /^ㅇ+$/,
    /^ㅎ+$/,
  ];

  // 의미있는 단어 필터링 (2글자 이상 + 필러가 아닌 것)
  const meaningfulWords = words.filter(word => {
    if (word.length < 2) return false;
    return !fillerPatterns.some(pattern => pattern.test(word));
  });

  const meaningfulRatio = meaningfulWords.length / wordCount;

  // 품질 기준:
  // minimal: 15단어 미만 또는 의미있는 단어 비율 30% 미만
  // sparse: 80단어 미만 또는 의미있는 단어 비율 50% 미만
  // rich: 그 외

  if (wordCount < 15 || meaningfulRatio < 0.3) return 'minimal';
  if (wordCount < 80 || meaningfulRatio < 0.5) return 'sparse';
  return 'rich';
}

// =============================================================================
// Prompts
// =============================================================================

// Universal Prompt - 할루시네이션 방지 및 입력 요청 방지 제약 추가
const UNIVERSAL_PROMPT = `당신은 전문 에디터이자 콘텐츠 요약 전문가입니다.
아래에 제공된 녹취록을 분석하여 핵심 내용을 파악하고, 읽기 쉬운 마크다운 문서로 정리해주세요.

## 녹취록 (이것이 정리해야 할 내용입니다)
{{transcript}}

## ⚠️ 절대 금지 사항
1. **녹취록에 없는 내용은 절대 추가하거나 추측하지 마세요.**
2. **"녹취록을 제공해주세요", "내용을 입력해주세요" 등 입력을 요청하는 응답을 하지 마세요.** 위에 녹취록이 이미 제공되어 있습니다.
3. 녹취록이 불완전하거나 내용이 부족해도:
   - 있는 내용만 간결하게 정리하세요
   - 억지로 내용을 채우거나 만들어내지 마세요
4. 불명확한 부분은 "[불명확]" 또는 "..." 으로 표시하세요
5. 녹취록에 언급되지 않은 세부사항을 창작하지 마세요

## 필수 요구사항
다음 규칙을 엄격히 준수하세요:

1. **제목**: 녹취록의 주제/맥락을 요약한 제목을 작성하세요. (녹취록 첫 문장을 그대로 복사하지 마세요)
2. **3줄 핵심 요약**: 제목 바로 아래에, 전체 내용을 관통하는 **가장 중요한 3가지 핵심 내용**을 요약해서 적어주세요. 이 부분은 반드시 있어야 합니다.
3. **유연한 본문 구성**: 3줄 요약 이후의 내용은 녹취록의 성격에 따라 자유롭게 구성하되, **반드시 소제목(##, ###)을 사용하여 구조화**해주세요. 줄글로만 길게 늘어놓지 마세요.
   - **회의라면**: ## 안건, ## 결정 사항, ## 액션 아이템 등으로 섹션 구분.
   - **강의라면**: ## 학습 목표, ## 주요 개념, ## 핵심 요점 등으로 섹션 구분.
   - **단순 대화라면**: ## 주요 대화 흐름, ## 결론 등으로 섹션 구분.
   - **억지스러운 구조화 금지**: 내용이 없는데 억지로 섹션을 만들지는 마세요. 하지만 가능한 한 구조적으로 정리해주세요.

## 출력 형식
반드시 아래 형식을 지켜주세요. 괄호 안의 설명은 실제 내용으로 대체해야 합니다:

[TITLE]
녹취록 내용을 요약한 실제 제목
[/TITLE]
[CONTENT]
### 📌 3줄 핵심 요약
- 첫 번째 핵심 내용
- 두 번째 핵심 내용
- 세 번째 핵심 내용

이후 본문 내용을 마크다운으로 작성 (적절한 이모지 사용 권장)
[/CONTENT]

⚠️ 주의:
- "(제목)", "(핵심 1)" 같은 플레이스홀더를 그대로 출력하지 마세요.
- "녹취록을 제공해주세요", "내용을 입력해주세요" 같은 입력 요청 문구를 절대 출력하지 마세요.
- 반드시 위에 제공된 녹취록을 기반으로 실제 내용을 작성하세요.`;

// Sparse Prompt - 짧은 녹취록용 (80단어 미만)
const SPARSE_PROMPT = `당신은 전문 에디터입니다.
아래 녹취록을 간결하게 요약해주세요.

## 녹취록
{{transcript}}

## ⚠️ 절대 금지 사항
1. **녹취록에 없는 내용은 절대 추가하지 마세요.**
2. **"녹취록을 제공해주세요" 같은 응답을 하지 마세요.**
3. **"녹음 내용이 짧습니다"라고만 말하고 끝내지 마세요.**
4. **제목이나 본문에 녹취록 원본을 그대로 복사하지 마세요.** 반드시 요약된 형태로.

## 요구사항
- 제목: 녹취록의 주제/맥락을 요약 (첫 문장 복사 금지)
- 핵심 내용을 1-2줄로 요약
- 본문도 요약된 형태로 작성 (원본 복사 금지)

## 출력 형식 (반드시 따르세요)
[TITLE]
녹취록 주제/맥락을 요약한 제목 (예: "날씨 이야기", "회의 안건 논의")
[/TITLE]
[CONTENT]
### 📌 핵심 내용
- (녹취록의 핵심 메시지를 요약)

### 📝 상세 내용
(녹취록 내용을 요약하여 정리)
[/CONTENT]

⚠️ 중요: 제목과 본문 모두 원본 복사 금지, 반드시 요약된 형태로 작성하세요.`;

// Minimal Prompt - 매우 짧은 녹취록용 (15단어 미만)
const MINIMAL_PROMPT = `당신은 전문 에디터입니다.
아래 녹취록이 아무리 짧아도 반드시 요약하여 정리해주세요.

## 녹취록
{{transcript}}

## ⚠️ 절대 금지 사항
- **녹취록을 그대로 복사하여 붙여넣기 금지** - 제목과 본문 모두 요약된 형태로 작성
- **제목에 녹취록 첫 문장을 그대로 쓰지 마세요** - 주제/맥락을 요약한 제목으로
- **"내용이 짧습니다", "내용이 부족합니다" 등의 메타 코멘트 금지**
- **"녹취록을 제공해주세요" 같은 입력 요청 금지**
- **녹취록에 없는 내용 추가 금지**

## 필수 요구사항
1. 제목: 녹취록의 주제나 맥락을 요약 (예: "날씨에 대한 이야기", "컨디션 이야기")
2. 본문: 녹취록 내용을 **요약하여** 작성 (원본 그대로 복사 금지)
3. 짧은 내용이라도 화자의 상황, 감정, 핵심 메시지를 파악하여 정리

## 출력 형식
[TITLE]
녹취록 주제/맥락을 요약한 제목 (첫 문장 복사 금지)
[/TITLE]
[CONTENT]
### 📌 핵심 내용
- (녹취록에서 파악한 핵심 메시지 1줄 요약)

### 📝 상세
(화자의 상황이나 맥락을 간단히 정리)
[/CONTENT]

⚠️ 중요: 제목과 본문 모두 녹취록 원본을 복사하지 말고, 반드시 요약된 형태로 작성하세요.`;

// =============================================================================
// Prompt Builders
// =============================================================================

export function buildUniversalPrompt(transcript: string): string {
  const date = formatKSTDate();
  return UNIVERSAL_PROMPT
    .replace("{{transcript}}", transcript)
    .replace("{{date}}", date);
}

export function buildSparsePrompt(transcript: string): string {
  return SPARSE_PROMPT.replace("{{transcript}}", transcript);
}

export function buildMinimalPrompt(transcript: string): string {
  return MINIMAL_PROMPT.replace(/\{\{transcript\}\}/g, transcript);
}

/**
 * 전사본 품질에 따라 적절한 프롬프트를 선택합니다.
 * @param transcript 전사본 텍스트
 * @returns 선택된 프롬프트와 품질 정보
 */
export function buildPromptByQuality(transcript: string): { prompt: string; quality: TranscriptQuality } {
  const quality = analyzeTranscriptQuality(transcript);

  switch (quality) {
    case 'minimal':
      return { prompt: buildMinimalPrompt(transcript), quality };
    case 'sparse':
      return { prompt: buildSparsePrompt(transcript), quality };
    case 'rich':
    default:
      return { prompt: buildUniversalPrompt(transcript), quality };
  }
}
